import { CreditBuilderPage } from "./CreditBuilderPage";

/** Pre-link — same layout as post; subtitle copy differs inside `CreditBuilderPage`. */
export function CreditPreConnection() {
  return <CreditBuilderPage variant="pre" />;
}
